export const EN_TEXT = {
  send: [/^Send$/i, /^Send message$/i],
  newChat: [/^New chat$/i, /^Start a new chat$/i],
  stopGenerating: [/^Stop generating$/i, /^Stop$/i],
  composer: [/message/i, /ask/i, /type a message/i],
  main: [/main/i, /chat/i]
} as const;
